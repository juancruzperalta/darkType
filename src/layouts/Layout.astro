---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/keyboard-solid.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<slot />
	</body>
</html>
<style is:global>
  :root{
    --text-color:rgb(224, 222, 222);
  }
	html {
		font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
		background: #13151a;
   color:var(--text-color);
   min-height: 100vh;
   padding-left: 1rem;
   padding-right: 1rem;
}
body{
      display:flex;
    align-items: center;
    justify-content: center;
}
  .wrapper{
    display: flex;
    width: 100%;
    flex-direction: column;
    min-height: 100vh;
    max-width: 1200px;
       padding-left: 1rem;
   padding-right: 1rem;
  }
  main{
    flex: 1
  }

  x-letter{
    position: relative;
    margin-left: 0.1rem;
    &.active::before{
      content: '|';
      color: #843dff;
      position: absolute;
      left: -.35rem;
      animation: keyUp 3s infinite ease-in-out;
    }
    &.Correct{
      color:rgb(209 213 219);
    }
    &.Incorrect{
      color:rgb(241, 11, 11);
    }

    &.active.is-last::before{
      left: 112%;
    }
  }
  x-word{
    &.marked{
      border-bottom: 1px solid rgb(241, 11, 11);
    }
    margin-left: .5rem;
  }
  @keyframes keyUp{
    25% {
      transform: scale(1.1);
    }
    50%{
      transform: scale(1.3);
    }
    25% {
      transform: scale(1.1);
    }
  }
    .topEdit{
      padding: 1rem;
    		background: #13151ad7;
        font-size: 1.3rem;
                  height: 80px;
      display: flex;
      align-items: center;
      justify-content: center;
  }
  .topEdit:first-child{
     margin-top: 1.5rem;
  }


  @media (min-width: 601px) and (max-width: 1024px) {
    .wrapper{
      max-width: 1000px;
    }
    .topEdit{
        font-size: 1.1rem;
  }
  }
  @media (max-width: 600px){
        .topEdit{
          padding:0.25rem;
          padding-top: 1rem;
          padding-bottom: 1rem;
    		background: #13151ad7;
        font-size: 1rem;
  }
  }
</style>
